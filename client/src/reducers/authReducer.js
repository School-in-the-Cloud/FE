const initialState = {
    isLoading: false,
    error: false
}

export const authentication = (state=initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}